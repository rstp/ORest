#!/usr/bin/env jruby

require 'jruby/core_ext'
#require 'facture'
require 'restinit'

Dir["lib\*.jar"].each { |jar| require jar }
$: << "src/main/ruby"

#Facture.new(ARGV).run


a = RESTorient.new
a.run
